{
	"name": "chitragupta-monorepo",
	"private": true,
	"type": "module",
	"version": "0.1.3",
	"license": "MIT",
	"description": "The Autonomous AI Agent Platform — 14-package TypeScript monorepo with zero-LLM cognitive layer, GraphRAG memory, bi-temporal knowledge graph, and self-evolving skills",
	"keywords": [
		"ai",
		"ai-agents",
		"autonomous-agents",
		"llm",
		"typescript",
		"mcp",
		"cli",
		"tool-use",
		"agent-framework",
		"multi-provider",
		"graphrag",
		"memory",
		"cognitive-ai"
	],
	"repository": {
		"type": "git",
		"url": "https://github.com/sriinnu/chitragupta.git"
	},
	"homepage": "https://github.com/sriinnu/chitragupta#readme",
	"bugs": {
		"url": "https://github.com/sriinnu/chitragupta/issues"
	},
	"scripts": {
		"clean": "npm run clean --workspaces --if-present && rm -rf dist",
		"build": "pnpm -r run build",
		"build:bundle": "pnpm -r run build && node scripts/bundle.mjs && node scripts/build-types.mjs",
		"dev": "concurrently --names \"core,swara,anina,smriti,ui,yantra,dharma,netra,vayu,sutra,cli\" \"cd packages/core && npm run dev\" \"cd packages/swara && npm run dev\" \"cd packages/anina && npm run dev\" \"cd packages/smriti && npm run dev\" \"cd packages/ui && npm run dev\" \"cd packages/yantra && npm run dev\" \"cd packages/dharma && npm run dev\" \"cd packages/netra && npm run dev\" \"cd packages/vayu && npm run dev\" \"cd packages/sutra && npm run dev\" \"cd packages/cli && npm run dev\"",
		"check": "biome check --write --error-on-warnings .",
		"test": "vitest run",
		"test:watch": "vitest",
		"test:coverage": "vitest run --coverage",
		"chitragupta": "tsx packages/cli/src/cli.ts",
		"start:serve": "tsx packages/cli/src/cli.ts serve --port 3141",
		"start:mcp": "tsx packages/cli/src/cli.ts mcp-server --transport sse --port 3001",
		"proxy": "tsx packages/darpana/src/bin/darpana.ts",
		"publish:dry": "bash scripts/publish.sh",
		"publish:real": "bash scripts/publish.sh --real"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.14",
		"@types/better-sqlite3": "^7.6.13",
		"@types/node": "^22.0.0",
		"better-sqlite3": "^12.6.2",
		"concurrently": "^9.2.1",
		"esbuild": "^0.25.0",
		"typescript": "^5.7.0",
		"vitest": "^4.0.18"
	},
	"engines": {
		"node": ">=22"
	},
	"pnpm": {
		"onlyBuiltDependencies": [
			"better-sqlite3",
			"esbuild"
		]
	},
	"dependencies": {
		"tsx": "^4.21.0"
	}
}
