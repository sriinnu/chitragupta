/**
 * @chitragupta/yantra — Tool system types.
 *
 * Re-exported from @chitragupta/core, which holds the canonical definitions
 * shared with @chitragupta/anina. This eliminates the duplicate ToolHandler
 * interface that previously existed across both packages.
 */

export type {
	ToolDefinition,
	ToolContext,
	ToolResult,
	ToolHandler,
} from "@chitragupta/core";
