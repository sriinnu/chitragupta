# Everything except dist/ — we publish from dist/ directly via `npm publish ./dist`
# This .npmignore is a safety net in case someone runs `npm publish` from root.
*
!dist/
!dist/**
!README.md
!LICENSE
